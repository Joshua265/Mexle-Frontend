import React, { FC } from "react";
import { Button } from "@material-ui/core";

const LicensePage: FC = () => {
  return (
    <React.Fragment>
      <h1>LicensePage</h1>
      <Button>Lizens</Button>
    </React.Fragment>
  );
};

export default LicensePage;
